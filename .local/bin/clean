#!/bin/zsh

dirs=(.nv .renpy .swt .java .pkg-cache .vcpkg .stellarium .mozilla .bitmonero)
files=(.yarnrc .wget-hsts .python_history .nvidia-settings-rc)

for dir in $dirs; do
    rm ~/$dir -rf
done

for file in $files; do
    rm ~/$file -f
done
