#!/bin/zsh

programs=(discord spotify qbittorrent chrome)

for program in $programs; do
    output=$(running $program)
    if [[ $? == 1 ]]; then
        echo "$program" >> $XDG_CACHE_HOME/xorg_state.txt
    fi
done
