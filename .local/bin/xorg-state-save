#!/bin/zsh

programs=(discord spotify qbittorrent chrome firefox)

for program in $programs; do
    output=$(running $program)
    if [[ $? == 1 ]]; then
        lines+="$program\n"
    fi
done
echo $lines | sed -z '$ s/\n$//' > "$XDG_CACHE_HOME/xorg_state.txt"
