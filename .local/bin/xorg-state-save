#!/bin/zsh

programs=(discord spotify qbittorrent chrome)

for program in $programs; do
    output=$(/home/norpie/.local/bin/running $program)
    if [[ $? == 1 ]]; then
        echo "$program" >> /home/norpie/.cache/xorg_state.txt
    fi
done
