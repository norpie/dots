require'shade'.setup({
    overlay_opacity = 50,
    opacity_step = 1,
    keys = {
        brightness_up    = '<C-Shift-Up>',
        brightness_down  = '<C-Shift-Down>',
        toggle           = '<Leader>ws',
    }
})
